Esegui la build di produzione di lets-night e analizza il risultato.

Esegui `npm run build` nella root del progetto.

Riporta:
- Successo o fallimento della build
- Tutti gli errori (bloccanti) con file e riga
- Warning importanti (es. immagini non ottimizzate, bundle troppo grande)
- Dimensione bundle per le route principali
- Pagine generate (static vs dynamic)

Se la build fallisce, suggerisci le correzioni necessarie.
